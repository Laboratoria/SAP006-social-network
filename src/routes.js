




























// import { pageMain } from './main.js';
// import { lala } from './pages/login/test.js'

// const routes = {
//     // '/': pageMain,
//     '/lala': lala,
// };
// const element = document.querySelector('#root');
// const validateHash = (hash) => (hash === '' ? '/' : hash.replace('#', ''));

// const routeRender = () => {
//     const pages = validateHash(window.location.pathname)

//     element.innerHTML = '';
//     element.appendChild(routes[pages]());
// };

// const initRoutes = () => window.addEventListener('hashchange', routeRender);

// window.addEventListener('load', () => {
//     routeRender()
//     initRoutes();
// });
// window.addEventListener('popstate', routeRender);